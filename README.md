\# sps\_genai - Word Embedding API



This project provides a FastAPI endpoint that returns a word embedding (vector) using spaCy's `en\_core\_web\_sm` model.



\## Run Locally (with uv)



```bash

uv run fastapi dev

